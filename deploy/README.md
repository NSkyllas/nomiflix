# deploy/

Deployment/setup notes and scripts for the VPS side: Jellyfin install steps (including disabling internet metadata providers in favor of local NFO — see `docs/SPEC.md` §6), systemd service files for the watcher script and the upload form (mirroring Nomify's `nomify-upload.service`), ufw rule additions, directory setup on `/opt/nomiflix/`.

Nothing here yet — fill in as the VPS setup happens.

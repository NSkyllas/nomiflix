# deploy/

Deployment/setup notes and scripts for the VPS side: Jellyfin install steps, systemd service files for the watcher script, ufw rule additions, directory setup on `/srv/nomiflix/` (or wherever the final mount ends up — see SPEC open questions).

Nothing here yet — fill in as the VPS setup happens.
